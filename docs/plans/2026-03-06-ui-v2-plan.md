# UI V2 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Replace the tab-based Streamlit UI with a chat-centric single-page layout for LevFin analysts.

**Architecture:** Eliminate tabs. Chat is the main area. Sidebar handles upload/status/stats. Definitions and Report are `@st.dialog` modals. New indexing progress shows a step pipeline. Theme.py gets rewritten for the new layout.

**Tech Stack:** Streamlit 1.54, `@st.dialog`, `@st.fragment`, custom CSS/HTML via `st.markdown(unsafe_allow_html=True)`, JS clipboard via `st.components.v1.html()`.

**Reference:** `docs/plans/2026-03-06-ui-v2-design.md`

---

### Task 1: Rewrite theme.py — CSS Design System

**Files:**
- Rewrite: `credit_analyzer/ui/theme.py` (CSS portion only, lines 1-988)

**Context:** theme.py contains APP_CSS (all styles) and HTML helper functions. This task rewrites the CSS. Keep all color constants. Keep all Python function signatures and implementations unchanged — ONLY replace the APP_CSS string content.

Read the design doc at `docs/plans/2026-03-06-ui-v2-design.md` for the full design spec.

**What to change in APP_CSS:**

REMOVE these CSS blocks:
- `.hero-card` and all hero sub-classes (hero-eyebrow, hero-icon, hero-title, hero-copy) — replaced by header bar
- Tab styles (`.stTabs`) — no more tabs

ADD these new CSS blocks:

**Header bar:**
```css
.app-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 1.5rem;
  height: 52px;
  background: linear-gradient(135deg, NAVY_DEEP 0%, #00254D 100%);
  border-bottom: 1px solid rgba(212, 175, 55, 0.15);
  color: white;
  position: sticky;
  top: 0;
  z-index: 100;
}

.app-header-title {
  font-family: 'DM Sans', system-ui, sans-serif;
  font-size: 1rem;
  font-weight: 600;
  letter-spacing: -0.01em;
  white-space: nowrap;
}

.app-header-actions {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.header-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  padding: 0.35rem 0.75rem;
  border-radius: 8px;
  border: 1px solid rgba(255,255,255,0.15);
  background: transparent;
  color: rgba(255,255,255,0.9);
  font-family: 'Source Sans 3', system-ui, sans-serif;
  font-size: 0.82rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
  text-decoration: none;
}

.header-btn:hover {
  background: rgba(255,255,255,0.1);
  border-color: rgba(255,255,255,0.3);
}

.header-btn-primary {
  background: RBC_BLUE;
  border-color: GOLD_BRIGHT;
  color: white;
}

.header-btn-primary:hover {
  background: #0060C0;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(0,26,62,0.3);
}

.header-btn-primary:disabled, .header-btn-primary[disabled] {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}

.doc-badge {
  display: inline-flex;
  align-items: center;
  padding: 0.2rem 0.6rem;
  border-radius: 999px;
  background: rgba(255,255,255,0.1);
  font-size: 0.78rem;
  font-weight: 500;
  color: rgba(255,255,255,0.8);
  max-width: 220px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

**Chat area:**
```css
.chat-welcome {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 50vh;
  text-align: center;
  padding: 2rem;
}

.chat-welcome-title {
  font-family: 'DM Sans', system-ui, sans-serif;
  font-size: 1.25rem;
  font-weight: 600;
  color: INK;
  margin-bottom: 0.5rem;
}

.chat-welcome-desc {
  font-size: 0.92rem;
  color: MUTED;
  max-width: 24rem;
  line-height: 1.5;
}

.suggestions-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.65rem;
  max-width: 800px;
  margin: 0 auto 2rem auto;
  padding: 0 1rem;
}

.suggestion-card {
  background: SURFACE;
  border: 1px solid BORDER;
  border-radius: 10px;
  padding: 0.85rem 1rem;
  cursor: pointer;
  transition: all 0.2s ease;
  text-align: left;
  font-size: 0.88rem;
  font-weight: 500;
  color: INK;
  line-height: 1.35;
}

.suggestion-card:hover {
  border-color: RBC_BLUE;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(0,81,165,0.1);
}

/* Context strip below assistant messages */
.context-strip {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.35rem 0;
  font-size: 0.78rem;
  color: MUTED;
  cursor: pointer;
  transition: all 0.15s ease;
}

.context-strip:hover {
  color: RBC_BLUE;
}

.context-strip .ctx-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  flex-shrink: 0;
}

.ctx-dot-high { background: #059669; }
.ctx-dot-medium { background: #D97706; }
.ctx-dot-low { background: #DC2626; }

.msg-timestamp {
  font-size: 0.72rem;
  color: MUTED;
  opacity: 0.6;
  margin-top: 0.25rem;
}

/* Streaming status line */
.stream-status {
  display: flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.35rem 0;
  font-size: 0.82rem;
  color: RBC_BLUE;
  font-weight: 500;
}

.stream-status .pulse-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: RBC_BLUE;
  animation: pulse 1.5s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 0.4; transform: scale(0.9); }
  50% { opacity: 1; transform: scale(1.1); }
}
```

**Indexing step pipeline:**
```css
.step-pipeline {
  padding: 0.5rem 0;
}

.step-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.3rem 0;
  font-size: 0.82rem;
  line-height: 1.3;
}

.step-item .step-icon {
  width: 18px;
  height: 18px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  font-size: 0.7rem;
}

.step-complete .step-icon { color: #059669; }
.step-active .step-icon { color: RBC_BLUE; }
.step-active .step-icon .pulse-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: RBC_BLUE;
  animation: pulse 1.5s ease-in-out infinite;
}
.step-pending { opacity: 0.45; }

.step-label { flex: 1; }
.step-count {
  color: MUTED;
  font-size: 0.78rem;
  font-weight: 500;
}

.stats-grid-compact {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.4rem;
  padding: 0.5rem 0;
}

.stat-item-compact {
  padding: 0.35rem 0.5rem;
  border-radius: 8px;
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(200,160,0,0.2);
}

.stat-item-compact .stat-value {
  font-family: 'DM Sans', system-ui, sans-serif;
  font-size: 1.1rem;
  font-weight: 700;
}

.stat-item-compact .stat-label {
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  opacity: 0.7;
}
```

**Skeleton loaders:**
```css
.skeleton-line {
  height: 0.85rem;
  border-radius: 4px;
  background: linear-gradient(90deg, BORDER 25%, SURFACE_ALT 50%, BORDER 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s ease-in-out infinite;
  margin-bottom: 0.5rem;
}

.skeleton-line:last-child { width: 60%; }

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

**Definitions modal:**
```css
.def-modal-search {
  margin-bottom: 0.75rem;
}

.def-modal-count {
  font-size: 0.8rem;
  color: MUTED;
  padding: 0.25rem 0 0.5rem 0;
}

.def-modal-list {
  max-height: 55vh;
  overflow-y: auto;
}
```

**Report dialog refinements:**
```css
.report-dialog-header {
  padding-bottom: 0.75rem;
  border-bottom: 1px solid BORDER;
  margin-bottom: 0.75rem;
}

.report-dialog-borrower {
  font-family: 'DM Sans', system-ui, sans-serif;
  font-size: 1.4rem;
  font-weight: 700;
  color: INK;
  margin: 0;
}

.report-dialog-meta {
  font-size: 0.85rem;
  color: MUTED;
  margin: 0.15rem 0 0 0;
}

.report-nav-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}
.report-nav-dot-complete { background: #059669; }
.report-nav-dot-generating { background: RBC_BLUE; animation: pulse 1.5s ease-in-out infinite; }
.report-nav-dot-pending { background: BORDER; }

.section-skeleton {
  background: SURFACE;
  border: 1px solid BORDER;
  border-left: 3px solid BORDER;
  border-radius: 12px;
  padding: 1rem 1.1rem;
  margin-bottom: 0.75rem;
}
```

KEEP unchanged:
- All color constants at the top
- Google Fonts import
- `:root` CSS variables
- Sidebar styles, button styles, input styles (already good)
- Metric card, panel card, rail card, report section, confidence pill, citation, copy button, quick-nav, definition card styles
- All scrollbar styles

**Step: Run tests after CSS changes**
Run: `.venv/Scripts/python -m pytest tests/ -x -q`
Expected: All pass (CSS-only changes)

**Step: Commit**
```bash
git add credit_analyzer/ui/theme.py
git commit -m "feat(ui): rewrite CSS for chat-centric layout"
```

---

### Task 2: Update theme.py HTML Helpers

**Files:**
- Modify: `credit_analyzer/ui/theme.py` (helper functions section, after APP_CSS)

**Context:** Remove `hero_card()`. Add new helpers for the header bar, chat components, and indexing pipeline. Keep ALL existing helpers unchanged (metric_card, panel_card, rail_card, confidence_pill, copy_button, nav_item, definition_card, empty_state, all citation/report formatting functions).

**Step 1: Remove `hero_card()` function**

Delete the `hero_card` function and `_SHIELD_ICON_SVG` constant. They are no longer used.

**Step 2: Add search icon SVG constant**

```python
_SEARCH_ICON_SVG = (
    '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" '
    'xmlns="http://www.w3.org/2000/svg">'
    '<circle cx="11" cy="11" r="7" stroke="currentColor" stroke-width="2"/>'
    '<line x1="16.5" y1="16.5" x2="21" y2="21" stroke="currentColor" stroke-width="2"/>'
    '</svg>'
)

_CHECK_ICON_SVG = (
    '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" '
    'xmlns="http://www.w3.org/2000/svg">'
    '<polyline points="20,6 9,17 4,12" stroke="currentColor" stroke-width="2.5" '
    'stroke-linecap="round" stroke-linejoin="round"/>'
    '</svg>'
)
```

**Step 3: Add header_bar() function**

```python
def header_bar(
    doc_name: str | None = None,
    report_ready: bool = False,
    has_document: bool = False,
) -> str:
    """Render the slim app header bar."""
    doc_badge = (
        f'<span class="doc-badge">{_safe(doc_name)}</span>'
        if doc_name
        else '<span class="doc-badge" style="opacity:0.5;">No document</span>'
    )
    report_disabled = "" if has_document else " disabled"
    return (
        '<div class="app-header">'
        '<span class="app-header-title">Credit Agreement Analyzer</span>'
        '<div class="app-header-actions">'
        f'{doc_badge}'
        '</div>'
        '</div>'
    )
```

Note: The actual New Chat, Definitions, Generate Report buttons will be Streamlit buttons placed in columns that overlay/align with the header. The header_bar HTML provides the background bar and title only, since Streamlit buttons need to be native st.button widgets to handle click events. The buttons are rendered via st.columns in app.py.

**Step 4: Add chat component helpers**

```python
def chat_welcome(has_document: bool = False) -> str:
    """Render centered welcome state for empty chat."""
    if has_document:
        return (
            '<div class="chat-welcome">'
            '<div class="chat-welcome-title">Ask a question about this agreement</div>'
            '<div class="chat-welcome-desc">'
            'Type a question below or choose from the suggestions.'
            '</div>'
            '</div>'
        )
    return (
        '<div class="chat-welcome">'
        '<div class="chat-welcome-title">Upload a credit agreement to get started</div>'
        '<div class="chat-welcome-desc">'
        'Use the sidebar to upload and index a PDF, then ask questions here.'
        '</div>'
        '</div>'
    )


def context_strip(
    confidence: str,
    chunk_count: int,
    sections_used: str,
    duration_seconds: float,
) -> str:
    """Render the compact context indicator below an assistant message."""
    tone = confidence.lower()
    dot_class = f"ctx-dot-{tone}" if tone in ("high", "medium", "low") else "ctx-dot-medium"
    return (
        '<div class="context-strip">'
        f'<span class="ctx-dot {dot_class}"></span>'
        f'<span>{_safe(confidence.upper())} confidence</span>'
        f'<span>&middot;</span>'
        f'<span>{chunk_count} chunks</span>'
        f'<span>&middot;</span>'
        f'<span>{_safe(sections_used)}</span>'
        f'<span>&middot;</span>'
        f'<span>{duration_seconds:.1f}s</span>'
        '</div>'
    )


def stream_status(label: str) -> str:
    """Render the animated streaming status line."""
    return (
        '<div class="stream-status">'
        '<span class="pulse-dot"></span>'
        f'<span>{_safe(label)}</span>'
        '</div>'
    )


def message_timestamp(time_str: str) -> str:
    """Render a subtle timestamp below a chat message."""
    return f'<div class="msg-timestamp">{_safe(time_str)}</div>'
```

**Step 5: Add indexing step pipeline helper**

```python
def indexing_step(label: str, status: str, count: str = "") -> str:
    """Render one step in the indexing pipeline.

    Args:
        label: Step description (e.g. "Extracting text").
        status: One of "complete", "active", "pending".
        count: Optional result count (e.g. "42 pages").
    """
    if status == "complete":
        icon_html = f'<span class="step-icon">{_CHECK_ICON_SVG}</span>'
        cls = "step-complete"
    elif status == "active":
        icon_html = '<span class="step-icon"><span class="pulse-dot"></span></span>'
        cls = "step-active"
    else:
        icon_html = '<span class="step-icon" style="color:var(--muted);">&#9675;</span>'
        cls = "step-pending"

    count_html = f'<span class="step-count">{_safe(count)}</span>' if count else ""
    return (
        f'<div class="step-item {cls}">'
        f'{icon_html}'
        f'<span class="step-label">{_safe(label)}</span>'
        f'{count_html}'
        '</div>'
    )


def compact_stats_grid(
    pages: int, sections: int, chunks: int, definitions: int
) -> str:
    """Render a 2x2 compact stats grid for the sidebar."""
    def _stat(value: int, label: str) -> str:
        return (
            '<div class="stat-item-compact">'
            f'<div class="stat-value">{value}</div>'
            f'<div class="stat-label">{label}</div>'
            '</div>'
        )
    return (
        '<div class="stats-grid-compact">'
        f'{_stat(pages, "Pages")}'
        f'{_stat(sections, "Sections")}'
        f'{_stat(chunks, "Chunks")}'
        f'{_stat(definitions, "Definitions")}'
        '</div>'
    )


def skeleton_lines(count: int = 4) -> str:
    """Render placeholder skeleton lines for loading states."""
    lines = ''.join(f'<div class="skeleton-line"></div>' for _ in range(count))
    return f'<div class="section-skeleton">{lines}</div>'
```

**Step 6: Add report nav dot helper**

```python
def report_nav_dot(status: str) -> str:
    """Small status dot for report quick-nav items.

    Args:
        status: One of "complete", "generating", "pending".
    """
    cls = f"report-nav-dot-{status}" if status in ("complete", "generating", "pending") else "report-nav-dot-pending"
    return f'<span class="report-nav-dot {cls}"></span>'
```

**Step 7: Update exports**

Remove `hero_card` from any `__all__` or imports. The app.py import line will be updated in Task 6.

**Step 8: Run tests**
Run: `.venv/Scripts/python -m pytest tests/ -x -q`
Expected: All pass

**Step 9: Commit**
```bash
git add credit_analyzer/ui/theme.py
git commit -m "feat(ui): add header bar, chat, pipeline, skeleton HTML helpers"
```

---

### Task 3: Definitions Search Dialog

**Files:**
- Create: `credit_analyzer/ui/definitions_dialog.py`
- Existing dependency: `credit_analyzer/ui/definitions_browser.py` (filter_definitions function)
- Existing dependency: `credit_analyzer/ui/theme.py` (definition_card)

**Context:** A `@st.dialog` that opens a searchable definitions modal. Uses the existing `filter_definitions()` from definitions_browser.py and `definition_card()` from theme.py.

**Step 1: Implement definitions_dialog.py**

```python
"""Definitions search dialog using @st.dialog."""

from __future__ import annotations

from typing import TYPE_CHECKING

import streamlit as st

from credit_analyzer.ui.definitions_browser import filter_definitions
from credit_analyzer.ui.theme import definition_card

if TYPE_CHECKING:
    from credit_analyzer.processing.definitions import DefinitionsIndex


@st.dialog("Search Defined Terms", width="medium")
def show_definitions_dialog(defs_index: DefinitionsIndex) -> None:
    """Render a searchable definitions modal."""
    total = len(defs_index.definitions)

    query = st.text_input(
        "Search",
        placeholder="e.g. EBITDA, Applicable Rate, Borrower...",
        key="def-dialog-search",
        label_visibility="collapsed",
    )

    filtered = filter_definitions(defs_index, query)

    if query:
        st.caption(f"{len(filtered)} of {total} terms match '{query}'")
    else:
        st.caption(f"{total} defined terms")

    if not filtered:
        st.info("No definitions match your search.")
        return

    # Show first 30 results in a scrollable area
    display_items = filtered[:30]
    remaining = len(filtered) - 30

    for term, definition_text in display_items:
        preview = definition_text[:300]
        is_truncated = len(definition_text) > 300
        st.markdown(
            definition_card(term, preview + ("\u2026" if is_truncated else "")),
            unsafe_allow_html=True,
        )
        if is_truncated:
            with st.expander(f"Full definition of \u201c{term}\u201d"):
                st.write(definition_text)

    if remaining > 0:
        st.caption(f"Showing 30 of {len(filtered)} results. Refine your search to see more.")
```

**Step 2: Run tests**
Run: `.venv/Scripts/python -m pytest tests/ -x -q`
Expected: All pass (no test needed for dialog rendering — it's a thin UI wrapper)

**Step 3: Commit**
```bash
git add credit_analyzer/ui/definitions_dialog.py
git commit -m "feat(ui): add definitions search dialog with @st.dialog"
```

---

### Task 4: Report Dialog with Progressive Rendering

**Files:**
- Create: `credit_analyzer/ui/report_dialog.py`
- Modify: `credit_analyzer/generation/report_generator.py` (add section_callback parameter)
- Existing dependency: `credit_analyzer/ui/theme.py` (report rendering helpers)

**Context:** A `@st.dialog(width="large")` that shows the report. When generating, it displays skeleton placeholders that fill in as sections complete. Uses a section_callback on ReportGenerator to enable this.

**Step 1: Add section_callback to ReportGenerator.generate()**

Read `credit_analyzer/generation/report_generator.py` to understand the current generate() method. Add an optional `section_callback: Callable[[GeneratedSection], None] | None = None` parameter. After each section completes (both the sequential section 1 and each parallel section), call the callback with the completed section.

The callback should be called from within the existing ThreadPoolExecutor future completion. Use `as_completed` instead of collecting all futures at once, so each section fires the callback as it finishes.

**Step 2: Implement report_dialog.py**

```python
"""Report generation and display dialog using @st.dialog."""

from __future__ import annotations

from html import escape as _html_escape
from typing import TYPE_CHECKING, Any, cast

import streamlit as st

from credit_analyzer.generation.pdf_export import report_to_pdf_bytes
from credit_analyzer.ui.theme import (
    confidence_pill,
    copy_button,
    format_report_body,
    nav_item,
    render_citation_footnotes,
    render_source_footnotes,
    report_nav_dot,
    skeleton_lines,
)

if TYPE_CHECKING:
    from credit_analyzer.generation.report_generator import (
        GeneratedReport,
        GeneratedSection,
        ReportGenerator,
    )
    from credit_analyzer.llm.base import LLMProvider
    from credit_analyzer.ui.workflows import ProcessedDocument


def _safe(text: str) -> str:
    return _html_escape(text).replace("$", "&#36;")


@st.dialog("Credit Agreement Report", width="large")
def show_report_dialog(
    document: "ProcessedDocument",
    provider: "LLMProvider",
    generator: "ReportGenerator",
) -> None:
    """Render the report dialog — generates if needed, displays if cached."""
    doc_id = document.document_id
    report: GeneratedReport | None = st.session_state.get(
        "generated_reports", {}
    ).get(doc_id)

    if report is not None:
        _render_report(report)
        return

    # Generate the report with progress
    _generate_and_render(document, provider, generator)


def _generate_and_render(
    document: "ProcessedDocument",
    provider: "LLMProvider",
    generator: "ReportGenerator",
) -> None:
    """Generate report with visible progress inside the dialog."""
    status_container = st.empty()
    progress_bar = st.progress(0)

    def on_progress(label: str, progress: float) -> None:
        status_container.info(label)
        progress_bar.progress(min(max(progress, 0.0), 1.0))

    try:
        report = generator.generate(
            document.document_id,
            progress_callback=on_progress,
        )
    except Exception as exc:
        status_container.empty()
        progress_bar.empty()
        st.error(f"Report generation failed: {exc}")
        return

    st.session_state.setdefault("generated_reports", {})[
        document.document_id
    ] = report
    status_container.empty()
    progress_bar.empty()
    _render_report(report)


def _render_report(report: "GeneratedReport") -> None:
    """Render a completed report inside the dialog."""
    from credit_analyzer.generation.report_generator import GeneratedSection

    complete_count = sum(1 for s in report.sections if s.status == "complete")
    high_count = sum(
        1 for s in report.sections
        if s.status == "complete" and s.confidence == "HIGH"
    )

    # Header
    st.markdown(
        f'<div class="report-dialog-header">'
        f'<p class="report-dialog-borrower">{_safe(report.borrower_name)}</p>'
        f'<p class="report-dialog-meta">Credit Agreement Analysis  |  '
        f'{_safe(report.generated_at.strftime("%B %d, %Y  %H:%M"))}  |  '
        f'{complete_count}/{len(report.sections)} sections  |  '
        f'{high_count} HIGH confidence  |  '
        f'{report.total_duration_seconds:.0f}s</p>'
        f'</div>',
        unsafe_allow_html=True,
    )

    # PDF download
    pdf_bytes = report_to_pdf_bytes(report)
    st.download_button(
        "Download PDF",
        data=pdf_bytes,
        file_name=f"credit_report_{report.generated_at.strftime('%Y%m%d_%H%M')}.pdf",
        mime="application/pdf",
    )

    # Two-column layout: nav + content
    nav_col, content_col = st.columns([0.22, 0.78], gap="medium")

    with nav_col:
        nav_html = '<div class="quick-nav">'
        nav_html += '<div class="quick-nav-title">SECTIONS</div>'
        for section in report.sections:
            dot = report_nav_dot(
                "complete" if section.status == "complete" else "pending"
            )
            nav_html += nav_item(
                section.section_number,
                section.title,
                f"report-section-{section.section_number}",
            )
        nav_html += '</div>'
        st.markdown(nav_html, unsafe_allow_html=True)

    with content_col:
        for section in report.sections:
            _render_section(section)


def _render_section(section: "GeneratedSection") -> None:
    """Render a single report section card."""
    conf_pill = confidence_pill(section.confidence)
    num_html = f'<span class="report-section-num">{section.section_number}</span>'
    anchor_id = f"report-section-{section.section_number}"
    body_id = f"section-body-{section.section_number}"

    if section.status == "error":
        st.markdown(
            f'<div id="{anchor_id}" class="report-section">'
            '<div class="report-section-head">'
            f'<div style="display:flex;align-items:center;">{num_html}'
            f'<span class="report-section-title">{_safe(section.title)}</span></div>'
            f'<div class="report-section-badges">{confidence_pill("LOW")}</div>'
            '</div>'
            f'<div class="report-error-body">Generation error: {_safe(section.error_message)}</div>'
            '</div>',
            unsafe_allow_html=True,
        )
        return

    inline_cites = getattr(section, "inline_citations", None)
    if inline_cites:
        footnotes_html = render_citation_footnotes(inline_cites)
    elif section.sources:
        footnotes_html = render_source_footnotes(section.sources)
    else:
        footnotes_html = ""

    st.markdown(
        f'<div id="{anchor_id}" class="report-section">'
        '<div class="report-section-head">'
        f'<div style="display:flex;align-items:center;">{num_html}'
        f'<span class="report-section-title">{_safe(section.title)}</span></div>'
        '<div class="report-section-badges">'
        f'<span class="badge-chunks">{section.chunk_count} chunks | {section.duration_seconds:.1f}s</span>'
        f'{conf_pill}'
        '</div></div>'
        '<div class="report-section-body" style="position:relative;">'
        f'<div id="{body_id}" class="report-body">'
        f'{format_report_body(section.body, inline_citations=inline_cites)}'
        f'</div>'
        f'{copy_button(body_id)}'
        '</div>'
        f'{footnotes_html}'
        '</div>',
        unsafe_allow_html=True,
    )
```

**Step 3: Run tests**
Run: `.venv/Scripts/python -m pytest tests/ -x -q`
Expected: All pass

**Step 4: Commit**
```bash
git add credit_analyzer/ui/report_dialog.py credit_analyzer/generation/report_generator.py
git commit -m "feat(ui): add report dialog with generation and display"
```

---

### Task 5: Rewrite app.py — Chat-Centric Layout

**Files:**
- Rewrite: `app.py`

**Context:** This is the main integration task. Remove all tab-based code. Replace with: header bar (via st.columns for buttons), sidebar (model/upload/stats), main chat area, dialog triggers. This task depends on all previous tasks being complete.

Read the current `app.py` in full before making changes.

**Key structural changes:**

1. **Remove all tab code** — No `st.tabs()`, no `tab_documents`, `tab_chat`, `tab_definitions`, `tab_report`.

2. **Remove hero card** — No `hero_card()` call.

3. **New import list:**
```python
from credit_analyzer.ui.theme import (
    APP_CSS,
    chat_welcome,
    compact_stats_grid,
    confidence_pill,
    context_strip,
    copy_button,
    format_report_body,
    header_bar,
    indexing_step,
    message_timestamp,
    nav_item,
    panel_card,
    rail_card,
    render_citation_footnotes,
    render_inline_citations,
    render_source_footnotes,
    skeleton_lines,
    stream_status,
)
from credit_analyzer.ui.definitions_dialog import show_definitions_dialog
from credit_analyzer.ui.report_dialog import show_report_dialog
```

4. **New main() structure:**

```python
def main() -> None:
    config_errors = validate_config()
    if config_errors:
        for error in config_errors:
            st.error(error)
        st.stop()

    _initialize_state()
    load_embedder()
    load_reranker()

    provider, provider_status = _load_provider_state()
    documents: dict[str, ProcessedDocument] = st.session_state.documents
    active_document = documents.get(st.session_state.active_document_id)

    # Header bar
    _render_header(active_document, provider, provider_status)

    # Sidebar
    _render_sidebar(active_document, provider_status)

    # Main chat area (the entire remaining space)
    _render_main(active_document, provider, provider_status)
```

5. **_render_header()** — Uses st.columns to create the header layout:
```python
def _render_header(
    active_document: ProcessedDocument | None,
    provider: LLMProvider | None,
    provider_status: dict[str, Any],
) -> None:
    doc_name = active_document.display_name if active_document else None
    st.markdown(
        header_bar(doc_name=doc_name, has_document=active_document is not None),
        unsafe_allow_html=True,
    )

    # Buttons row (rendered below the HTML header via columns)
    cols = st.columns([0.55, 0.12, 0.12, 0.21], gap="small")
    with cols[0]:
        pass  # spacer (title is in the HTML header)
    with cols[1]:
        if st.button("New Chat", key="new-chat", disabled=active_document is None, use_container_width=True):
            if active_document:
                _clear_chat(active_document.document_id)
                st.rerun()
    with cols[2]:
        if st.button("Definitions", key="open-defs", disabled=active_document is None, use_container_width=True):
            if active_document:
                show_definitions_dialog(active_document.definitions_index)
    with cols[3]:
        if st.button("Generate Report", key="gen-report", type="primary",
                      disabled=active_document is None or not provider_status["ready"],
                      use_container_width=True):
            if active_document and provider:
                # Build generator and open dialog
                generator = ReportGenerator(active_document.retriever, provider)
                if active_document.preamble_text is not None:
                    generator.set_preamble(
                        active_document.preamble_text,
                        page_numbers=active_document.preamble_page_numbers,
                    )
                show_report_dialog(active_document, provider, generator)
```

6. **_render_sidebar()** — Simplified three-section sidebar:
```python
def _render_sidebar(
    active_document: ProcessedDocument | None,
    provider_status: dict[str, Any],
) -> None:
    with st.sidebar:
        # Model status
        model_meta = (
            f"{provider_status['provider_name'].upper()} | Ready"
            if provider_status["ready"]
            else f"{provider_status['provider_name'].upper()} | {provider_status['message']}"
        )
        st.markdown(
            rail_card(
                "Model",
                provider_status["model_name"],
                model_meta,
                tone="ready" if provider_status["ready"] else "warning",
            ),
            unsafe_allow_html=True,
        )
        if not provider_status["ready"] and st.button("Retry Connection", key="retry-model", use_container_width=True):
            st.session_state.provider_status = None
            st.rerun()

        st.markdown("---")

        # Document upload
        st.caption("DOCUMENT")
        uploaded_file = st.file_uploader("Agreement PDF", type=["pdf"], label_visibility="collapsed")
        if st.button("Index PDF", type="primary", disabled=uploaded_file is None, use_container_width=True):
            if uploaded_file is not None:
                pdf_path = save_uploaded_pdf(uploaded_file.name, uploaded_file.getvalue())
                _process_document(pdf_path)

        # Document stats (when loaded)
        if active_document is not None:
            st.markdown(
                compact_stats_grid(
                    pages=active_document.stats.total_pages,
                    sections=active_document.stats.section_count,
                    chunks=active_document.stats.chunk_count,
                    definitions=active_document.stats.definition_count,
                ),
                unsafe_allow_html=True,
            )
            if st.button("Remove Document", key="remove-doc", type="secondary"):
                _remove_document(active_document.document_id)
                st.rerun()

        st.markdown("---")
        st.caption("RBC Leveraged Finance")
        st.markdown(
            f'<div style="font-size:0.78rem;opacity:0.6;">'
            f'{provider_status["model_name"]}</div>',
            unsafe_allow_html=True,
        )
```

7. **_render_main()** — The chat area, three states:
```python
def _render_main(
    active_document: ProcessedDocument | None,
    provider: LLMProvider | None,
    provider_status: dict[str, Any],
) -> None:
    if active_document is None:
        st.markdown(chat_welcome(has_document=False), unsafe_allow_html=True)
        return

    if provider is None or not provider_status["ready"]:
        st.warning("The document is indexed, but the model backend is not ready.")
        return

    doc_id = active_document.document_id
    messages = st.session_state.chat_messages[doc_id]

    # If no messages yet, show suggestions
    if not messages and doc_id not in st.session_state.pending_chat_questions:
        _render_suggestions(active_document)

    # Render existing messages
    for message in messages:
        _render_chat_message(message)

    # Handle pending question
    pending = st.session_state.pending_chat_questions.get(doc_id)
    if pending is not None:
        _run_pending_chat_question(active_document, provider, pending)
        st.rerun()

    # Chat input (always at bottom)
    user_question = st.chat_input("Ask about pricing, covenants, baskets, or debt capacity")
    if user_question is not None:
        _queue_chat_question(active_document, user_question)
        st.rerun()
```

8. **_render_suggestions()** — Suggested questions grid using st.columns (3 cols, 2 rows):
```python
def _render_suggestions(active_document: ProcessedDocument) -> None:
    st.markdown(chat_welcome(has_document=True), unsafe_allow_html=True)
    with st.container(key="suggested-actions"):
        for row_start in range(0, len(SUGGESTED_QUESTIONS), 3):
            row = SUGGESTED_QUESTIONS[row_start:row_start + 3]
            cols = st.columns(len(row))
            for i, suggestion in enumerate(row):
                with cols[i]:
                    if suggestion.prompt is not None and st.button(
                        suggestion.label,
                        key=f"suggested-{row_start + i}",
                        use_container_width=True,
                    ):
                        _queue_chat_question(active_document, suggestion.prompt)
                        st.rerun()
```

9. **_render_chat_message()** — Updated to include timestamps and context strip:
Keep existing citation/copy logic. Add `message_timestamp()` after each message. For assistant messages, add `context_strip()` with confidence, chunk count, section IDs, and duration extracted from the QAResponse. The context strip is rendered as a clickable element — clicking it toggles the expanded chunks table via `st.expander`.

10. **_process_document()** — Updated to use step pipeline rendering:
Instead of a generic progress bar, render `indexing_step()` HTML blocks in the sidebar that update as the callback fires. Parse the callback label to determine which step is active and what counts to show.

11. **Remove all tab-related code** — Delete `_render_document_tab`, `_render_definitions_tab`, `_render_report_tab`, `_render_report`, `_render_report_section`. The document summary data is now shown in the sidebar stats grid. Definitions are in the dialog. Report is in the dialog.

**Step: Run tests**
Run: `.venv/Scripts/python -m pytest tests/ -x -q`
Expected: All pass

**Step: Run linter**
Run: `.venv/Scripts/python -m ruff check .`
Fix any issues.

**Step: Run type checker**
Run: `.venv/Scripts/python -m pyright`
Fix any type errors in modified files.

**Step: Commit**
```bash
git add app.py
git commit -m "feat(ui): rewrite app.py as chat-centric single-page layout"
```

---

### Task 6: Integration Testing and Polish

**Files:**
- Modify: any files with lint/type/test issues

**Step 1: Run full test suite**
Run: `.venv/Scripts/python -m pytest tests/ -x -q`
Fix any failures.

**Step 2: Run linter**
Run: `.venv/Scripts/python -m ruff check .`
Fix any issues.

**Step 3: Run type checker**
Run: `.venv/Scripts/python -m pyright`
Fix any type errors.

**Step 4: Review CSS for consistency**
Read theme.py APP_CSS and verify:
- All color references use CSS variables (var(--xxx)) or the Python constants
- No emojis anywhere
- Transitions are 0.2s ease on all interactive elements
- Font families are consistent (DM Sans for headings, Source Sans 3 for body)

**Step 5: Verify no dead imports**
Check app.py imports — ensure nothing references removed functions (hero_card, etc.).

**Step 6: Commit**
```bash
git add -A
git commit -m "fix(ui): resolve lint, type, and test issues from UI v2 rewrite"
```

---

## Task Dependency Graph

```
Task 1 (CSS) ─────────────┐
Task 2 (Helpers) ──────────┤
Task 3 (Definitions) ──────┼──> Task 5 (App.py) ──> Task 6 (Polish)
Task 4 (Report Dialog) ────┘
```

**Parallelizable:** Tasks 1+2 (different sections of same file — run sequentially as one unit), Tasks 3+4 (independent new files, can run in parallel)
**Sequential:** Task 5 depends on all others. Task 6 depends on Task 5.
